package task04;

public class ClockAction {
    public void setTime(int hour, int minute, int second,Clock clock){
        clock.setHour(hour);
        clock.setMinute(minute);
        clock.setSecond(second);
    }
}
